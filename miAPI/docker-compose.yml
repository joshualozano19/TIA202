 services:
  api:
    build: .
    container_name: server_FastAPI
    ports:
      - "5000:5000"
    volumes:
     - ./app:/app
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "5000", "--reload"]